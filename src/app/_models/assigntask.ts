//import { DatePipe } from "@angular/common";

export class AssignTask {
    taskId: number;
    name: string;
    description: string;
    startDate: Date;
    endDate: Date;
    userId: number;
    assignedUserName: string;
}